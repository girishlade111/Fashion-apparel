import type { Config } from "tailwindcss";

// Tailwind CSS v4 uses CSS-based configuration via @theme.
// This file is kept for compatibility with shadcn/ui tooling.
const config: Config = {
  content: [],
};

export default config;
